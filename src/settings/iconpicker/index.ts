/**
 * settings/iconpicker/index.ts — Public entry point for the icon picker.
 *
 * Callers only ever need the modal; the panel and grid are internal.
 */
export { IconPicker } from "./IconPickerModal";
export type { IconPickerPlugin } from "./IconPickerModal";
